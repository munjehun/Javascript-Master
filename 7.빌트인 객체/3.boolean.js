// Boolean
console.log(true.valueOf()); //boolean 타입도 객체타입이 있다. (valueOf =값 반환)
const isTrue = true;

/**
 * Falshy
 * 0
 * -0
 * null
 * NaN
 * undefined
 * ''
 */

/**
 * Fruthy
 * 1
 * -1
 * '0'  => 0 이라는 string이 들어가 있어서
 * 'false' => false 라는 string이 들어가 있어서
 * [] => 빈 배열
 * {} => 빈 객체
 */
