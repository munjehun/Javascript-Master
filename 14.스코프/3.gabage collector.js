//  글로벌 변수는 앱이 종료될때까지 메모리에 유지됨.
//  => 이름 충돌 면에서도, 메모리 사용 면에서도 글로벌 변수는 좋지 않음!
const global = 1;

{
  // 블럭 내부에만 존재하고, 블럭이 끝나면 자동 소멸
  const global = 1;
}

function print() {
  // 함수 내부에서도 블럭 안에서 필요한 경우엔 필요한 블럭 안에서 선언하고 사용하기 !
  if (true) {
    let temp = 0;
  }
}
