// 산술 연산자 (Arithmetic operrator)
// +
// -
// *
// /
// % 나머지 연산자
// ** 지수(거듭제곱) 연산자  (es7)

// +연산자 주의점!
let text = "두개의" + "문자를";
console.log(text);
text = "1" + 1; //숫자와 문자열을 더하면 문자열로 변환됨!
console.log(text);
